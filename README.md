# Contact List Manager (Assignment 6)

Flask-based contact list manager used for automated testing with pytest and GitHub Actions.

## Setup (Windows)

```bat
python -m venv venv
venv\Scripts\activate
python -m pip install -r requirements.txt
